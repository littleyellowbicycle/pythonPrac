print 语句后接逗号不换行

row_input(string) 直接读取控制台的输入（任何类型的输入它都可以接收）,raw_input() 将所有输入作为字符串看待，返回字符串类型
input(type)能够读取一个合法的 python 表达式，即你输入字符串的时候必须使用引号将它括起来，否则它会引发一个 SyntaxError
nput() 在对待纯数字输入时具有自己的特性，它返回所输入的数字的类型（ int, float ）
